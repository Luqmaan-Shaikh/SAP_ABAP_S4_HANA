## Day 32 (16/03/2026) :-

## RAP (Restful Application Programming) :-

-> ABAP RESTful Application Programming Model currently has 3 scenarios
-> The scenarios are based on what the developer has to implement i.e. what the developer manages vs what SAP RAP Framework manages

1. Managed :-
-> The CRUD operations are managed by SAP

2. Unmanaged :-
-> The CRUD operations are implemented by Developer in the class associated with the behaviour definition

3. Managed with Unmanaged Save :-
-> The CRUD operations are managed by SAP however, the developer can decide to implement CRUD operations for one of the entities from the data model

-> In this post you will learn about implementing Managed with unmanaged save
-> The data model used for this application is Carrier and Connection Entities from the SAP Flight Model

# Create CDS View Entities :-

-> Create below 2 CDS View Entities and achieve them together 
-> As these entities refer to each other, you have to activate these together
-> The images for better readability and code text for copying both are provided


## Building ABAP Restful Application :-

# Provide the Database Table :-
-> Developing a RAP stats with providing the DBT 
-> Depending on the development on whether it is a simple or composite Business Object, one or more CDS Views are 

-> The data model of the BO is defined with CDS Views
-> Depending on whether it is a simple or a composite BO 

## Project the RESTful Application Programming Business Object and provide service-specific Metadata :-

-> The RESTful Application Programming BO projection consists of a data model projection and if  


## Creation of the Sustainable Grocery Application 

1. Define the Database Table 
2. Generate the Additional Objects
3. Define and implement the Behaviour 
4. Adjust the UI in the Metadata Extension

-> To create the RAP , the foll objects needs to be created or generated :-

1. A DBT
-> 

2. Additional Objects 
-> A number of objects required by the RAP will be generated through the Generate AAP Repository Objects .. feature of ABAP Development Tools (ADT)


3. Behaviour :- 
-> The behaviour of a RAP BO is defined in a repository object called a CDS Behaviour Definition 
-> Usually, the behaviour also requires additional logic, which is implemented in a certain type of global ABAP class called Behaviour Pool 
-> For a non transactional appln for ex a list report the behaviour definition or implementation can be omitted
-> The behaviour we will add is an action to check if the grocery items selected have expired 
-> This will set the expired flag in the DBT

4. Metadata Extension :-
-> Metadata Extension File allows customization of the User Interface

## Create a DBT :-

# Steps :-

1. Create a DBT named ZLUQ_GROCERY
  a. Right-click on your package Z... in the Project Explorer and choose New->Object ABAP Repository Object Type table in the filter and choose DBT. Press Next
  b. Enter ZLUQ_GROCERY as the name and Groceries as the description. Press Next to continue

2. Adjust the ZLUQ_GROCERY file by copying the template code and replacing all occurrences of LUQ with your nukber
  a. Replace the generated code in ZLUQ_GROCERY with the foll code 


## Generating Additional Objects :-

-> We have just defined a DBT to hold our data, but the app we will create nodes more objects that just this table 
-> We can generate the objects using a wizard in ABAP Development Tools (ADT)

-> The generated objects contain all the information necessary to provide a working app with create, read update and delete capabilities 
-> Later on, we will also adjust and extend some of these objects to change the appearance of the user interface and to implement a check for expired groceries 

-> To start the object generator, right-click the table name in the Project Explorer and choose Generate ABAP Repository Objects 
-> The wizard starts, and you must enter a package to which all of the new objects will be assigned 
-> You then select the generator, for this example we will use the ABAP RESTful Application Programming Model UI Service

## Standard Operations :-

-> The behaviour for standard operations can be provided by the RAP Framework Automatically

- Create, Update, Delete -> These are the primary standard operation. These are also called as Modify Operations
- Create by Association -> This is an Update Operation that creates new instances of an associated entity
- Lock -> This is a standard operation that locks instances of a Business Object Entity

## Nonstandard Operations :-

-> These operations are used to provide customized behaviour based on business requirements

## Actions :-

-> You can provide action buttons for the actions on the list report or an object page
-> For Ex, in the travel app that is built earlier, we can add a set status button to change the status to blocked 

- The action can be triggered in one of the three ways mentioned below :-

1. From the service consumer i.e. by the application user interface 
2. Internally by another action or by a determination via EML
3. By other business objects via EML


















